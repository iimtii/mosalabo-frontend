apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: network.jpe1a.dcnw.rakuten
  namespace: cc-tech-newgrad-f
spec:
  podSelector:
    matchLabels:
      networkpolicy.ecbs-bos.rakuten/network-policy: allow
  policyTypes:
    - Egress
  egress:
    - ports:
        - port: 80
          protocol: TCP
      to:
        - ipBlock:
            cidr: 100.99.126.0/24
