import { Layout } from "../components/common/Layout";

import { Heading } from "../components/Heading";

const Home = () => {
  return (
    <Layout>
      <Heading />
    </Layout>
  );
};

export default Home;
